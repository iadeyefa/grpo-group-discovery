# Method 4: Latent class preference model — not yet implemented.

dataset:
  name: Anthropic/llm_global_opinions
  cache_dir: null
  source_groups: null

entities:
  mode: simulated
  n_simulated_per_group: 10
  random_state: 42

clustering:
  method: latent_class
  n_clusters: 5
  max_iter: 100
  tol: 0.0001
  smoothing: 0.001
  random_state: 42

export:
  output_dir: outputs
  run_name: latent_class
  dataset_prefix: goqa_cluster
